import HeaderEmpty from '@components/Header/HeaderEmpty'
import Footer from '@components/Footer/Footer'

const ParentLogin = () => {
  return (
    <div>
      <HeaderEmpty />
      <div>
        {/* for test */}
        학부모 핸드폰 번호로 로그인, 학생 아이디/비번으로 로그인
        두 로그인 방식 지원?
      </div>
      <Footer />
    </div>
  )
}

export default ParentLogin
